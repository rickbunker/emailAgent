# Memory System Configuration
# Copy this to .env and set your preferred configuration

# Option 1: Use local sentence transformers (default)
# The model will be cached after first download
SENTENCE_TRANSFORMERS_HOME=~/.cache/sentence_transformers
TRANSFORMERS_CACHE=~/.cache/huggingface
HF_HOME=~/.cache/huggingface

# Option 2: Use OpenAI embeddings (recommended for production)
# Uncomment these lines and add your API key
# USE_OPENAI_EMBEDDINGS=true
# OPENAI_API_KEY=your-api-key-here
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Memory limits (production values)
SEMANTIC_MEMORY_MAX_ITEMS=50000
PROCEDURAL_MEMORY_MAX_ITEMS=10000
EPISODIC_MEMORY_MAX_ITEMS=100000
CONTACT_MEMORY_MAX_ITEMS=25000

# Qdrant configuration
QDRANT_HOST=localhost
QDRANT_PORT=6333

# Embedding model for sentence transformers
EMBEDDING_MODEL=all-MiniLM-L6-v2
